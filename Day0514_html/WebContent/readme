html 요소는 두가지로 분류
block 요소: 화면의 가로영역을 모두 차지하는 요소
in-line 요소: 내용만큼만 영역을 차지하기 때문에 옆으로 다른 요소가 올  수 있음

lang="ko" : 한글로 되어있는거 검색 
<h>: 문서내에서 제목을 표시할 때 사용, 1~6까지 중요순으로 사용
<p>: 문서에서 문장의 단락을 만드는 역할
<b>,<strong>: 텍스트 진하게(bold)
<mark>: 특정 텍스트 강조
<blockquote>: 인용문 작성할때에 사용 
<br>: 한 줄 개행 태그
<hr>: 한 줄 개행 ,줄 긋기 
<i>: 
<em>:
<span>: 요소 내에서 특정 영역을 지정할 때 사용
<pre>: 문자 그대로 화면에 표시하고 싶을때 사용, 내가 적은대로 다 나온다

src: 그림소스
alt: 그림 설명 
<figcaption>: 특정 영역을 구분하기 위해서 사용

list

ol: 순서가 있는 리스트(번호가 있음)
ul: 순서가 없는 리스트(번호가 없음)
il: 목록의 내용을 표시하는 태그

dl,dt,dd 제목,설명이 한쌍을 이루는 리스트
anchor: 웹 문서간 이동이나, 문서가 길때 문서내 이동
id 속성이 html 문서 내에서 요소들을 구분하는 구분자를 역할을 함 문서내에서 유일한 값을 가져야한다.
(사실은 문서 뿐아니라 프로젝트에 있는 모든 문서내에서 유일한 값을 가지면 좋다.)]

a href="목적지 주소" #<--아이디 를 의미 한다.

target 속성: 링크시 화면 표시방법을 ㅅ ㅓㄹ정
_blank: 새탭 또는 새창에서 열기
_self: 기본값, 현재 화면에서 링크가 열림
_parent: 프레임을 사용했을 경우에 내용을 부모 프레임에 표시


table : 데이터를 표 형식으로 표현하고 싶을 때 사용하는 요소
테스트 데이터를 화면에 표시할 때도 사용
전체적인 레이아웃을 잡을 때도 사용
tr : 가로행
th:칼럼제목
colspan: 셀병합


style 속성은 따로 지정 할 수 있다.
css(Cascaing Style sheet)

th와 td가 공통 style 속성을 가지니까 따로 모아 놓고 설정


<form> : 사용자로 부터 값을 입력받고 서버로 전송하기 위한 태그
하위태그로<input> 태그를 가지면서 정보를 입력받을 수 있다.
<input type = "종류">
type 종류:
text: 문자열
password: 문자열, 가려져서 화면출력
email: text와 동일인데 입력패턴을 검사해서 email주소 인지 아닌지 검사
submit: form 하위 요소의 내용을 서버로 전송하는 버튼,
	form 요소의 action에 선언된 요청이 실행
button: 버튼 모양의 요소 출력
reset: 버튼, form 하위 input 요소의 내용을 모두비움
checkbock: 항목출력, 다중 선택 가능한 체크박스
radio: 항목출력, 항목중 하나만 선택가능한 체크박스
file: 파일 업로드를 위해서 파일 선택가능한 요소
number: 숫자입력(최대,최소 등을 지정할 수 있다)
date: (날짜 입력 요소)


margin : 다른요소간의 거리